#include <stdio.h>
#include <math.h>

//Find the missing x1 and y1 coordinates
//Vector Mathematics 

int
main ()
{
  double a[10] = { };
  double b[10] = { };
  double v[10] = { };

  printf ("Enter the numbers for x2 and then y2 :");
  scanf ("%lf %lf", &b[0], &b[1]);
  printf ("Enter the numbers for the vector x and then y :");
  scanf ("%lf %lf", &v[0], &v[1]);
/*
 b[0] = b[0] - 0;
  b[1] = b[1] - 0;*///checking accurracy of equation

  a[0] = b[0] - v[0];		//finding the value of a ={x1,y1}
  a[1] = b[1] - v[1];
  printf ("The missing x1 coordinate is : %.f\n", a[0]);
  printf ("the missing y1 coordinate is : %.f\n", a[1]);


  return 0;
}
